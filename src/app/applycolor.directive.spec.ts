import { ApplycolorDirective } from './applycolor.directive';

describe('ApplycolorDirective', () => {
  it('should create an instance', () => {
    const directive = new ApplycolorDirective();
    expect(directive).toBeTruthy();
  });
});
